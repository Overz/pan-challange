openapi: 3.0.3
info:
  title: "Documentação do módulo 'Challange'"
  description: |
    "Alguma coisa"
  version: "0.0.1"
servers:
  - url: 'https://localhost:3000'
    description: "servidor de testes locais"
paths:
  "/":
    get:
      description: "bunda"
      responses:
        200:
          description: "retorna a bunda"
          content:
            "application/json":
              schema:
                properties:
                  batata:
                    type: string
                    description: "batata"
