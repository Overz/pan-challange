openapi: 3.0.3
info:
  title: "Documentação do módulo @project.artifactId@"
  description: |
    "@project.description@"
  version: "@project.version@"
servers:
  - url: 'https://localhost:3000'
    description: "servidor de testes locais"
paths:
  "/":
    get:
      description: "bunda"
      responses:
        200:
          description: "retorna a bunda"
          content:
            "application/json":
              schema:
                properties:
                  batata:
                    type: string
                    description: "batata"
